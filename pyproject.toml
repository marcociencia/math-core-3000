[build-system]
requires = ["setuptools>=42", "wheel", "pybind11>=2.10.0"]
build-backend = "setuptools.build_meta"

[project]
name = "math-core-3000"
version = "1.0.0"
description = "Super Calculator with C++ Backend"
requires-python = ">=3.7"
